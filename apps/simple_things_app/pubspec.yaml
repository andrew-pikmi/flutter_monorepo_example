name: simple_things_app
description: "A new Flutter project."

version: 1.1.0-dev.1

environment:
  sdk: ^3.5.2

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  simple_calculator: ^0.0.2-dev.1
  ui_kit: ^0.0.2-dev.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true