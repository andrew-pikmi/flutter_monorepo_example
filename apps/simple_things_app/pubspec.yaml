name: simple_things_app
description: "A new Flutter project."

version: 1.2.0

environment:
  sdk: ^3.5.2

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  simple_calculator: ^0.0.2+1
  simple_tap_speed_test: ^0.0.2
  ui_kit: ^0.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
