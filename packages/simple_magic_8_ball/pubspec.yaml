name: simple_magic_8_ball
description: "A simple Flutter magic 8 ball package."
version: 0.0.2-dev.0

environment:
  sdk: ^3.5.2
  flutter: ">=1.17.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  ui_kit: ^0.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
