name: dev version

on:
  push:
    branches:
      - develop

concurrency:
  group: version-${{ github.workflow }}-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  verify:
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.PAT || github.token }}

      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: fvm setup
        run: |
          curl -fsSL https://fvm.app/install.sh | bash
          echo "$HOME/fvm/bin" >> $GITHUB_PATH

      - name: dependencies install
        run: |
          npm ci
          fvm use
          fvm flutter pub get
          fvm dart run melos bs

      - name: nx run-many
        run: npx nx run-many -t format,analyze,test --all --outputStyle=static

  version:
    runs-on: ubuntu-latest
    needs: verify

    permissions:
      contents: write

    steps:
      - name: checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.PAT || github.token }}

      - name: fvm setup
        run: |
          curl -fsSL https://fvm.app/install.sh | bash
          echo "$HOME/fvm/bin" >> $GITHUB_PATH

      - name: install dependencies
        run: |
          fvm use
          fvm flutter pub get
          fvm dart run melos bs

      - name: version
        run: |
          git config user.name "github-actions" 
          git config user.email "github-actions@github.com"
          fvm dart run melos version -p --preid dev --yes \
            --message $'chore(release): publish packages [skip ci]\n{new_package_versions}'
          git push --follow-tags
